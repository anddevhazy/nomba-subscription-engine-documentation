export const meta = {
  eyebrow: "For subscribers",
  title: "Manage your subscription",
  lede: "Everything you can do from the portal, and why it doesn't touch the merchant's dashboard to do it.",
};

<p>The customer portal is authenticated separately from the merchant's dashboard — your login has nothing to do with the merchant's account, and nothing you do here requires them to act on it. That separation is deliberate: a subscription is yours to manage, not a request you file with someone else.</p>

<h2 id="h-actions">What you can do</h2>
<div className="card-grid cols-2">
  <div className="card"><div className="card-icon">👀</div><div className="card-title">View your subscription</div><div className="card-desc">Current plan, price, next billing date, and payment history.</div></div>
  <div className="card"><div className="card-icon">⏸️</div><div className="card-title">Pause</div><div className="card-desc">Stop billing without cancelling outright. Resume whenever you're ready.</div></div>
  <div className="card"><div className="card-icon">▶️</div><div className="card-title">Resume</div><div className="card-desc">Billing picks back up on your original cycle date.</div></div>
  <div className="card"><div className="card-icon">✋</div><div className="card-title">Cancel</div><div className="card-desc">Ends the subscription. What happens to the current billing period depends on the merchant's cancellation policy.</div></div>
</div>
<div className="card-grid cols-1">
  <div className="card"><div className="card-icon">💳</div><div className="card-title">Update your payment method</div><div className="card-desc">Your card expired, or you just want to switch which one gets charged. The new card is tokenised the same way as at signup — the portal never sees or stores the raw card number.</div></div>
</div>

<h2 id="h-signin">Signing in</h2>
<p>Phone or email plus a one-time code — no separate password to lose. The portal recognises you by the identity the merchant registered you under at signup.</p>

<h2 id="h-pause-vs-cancel">Pause vs. cancel</h2>
<p>Pausing is the better move if you just don't need the service this month but expect to come back — no re-onboarding, no re-entering a card, billing resumes exactly where it left off. Cancelling ends things properly; if you change your mind later, you subscribe again as a new signup.</p>

<div className="callout tip"><div className="ic">💡</div><p>If the only reason you're thinking about cancelling is a failed payment you haven't gotten around to fixing, updating your payment method is usually the faster path back to normal — see <a href="/subscribers/when-a-payment-fails">When a payment fails</a>.</p></div>

<h2 id="h-next">Next</h2>
<div className="card-grid cols-2">
  <a className="card" href="/subscribers/when-a-payment-fails"><div className="card-icon">🔁</div><div className="card-title">When a payment fails</div><div className="card-desc">The recovery flow, from your side.</div></a>
  <a className="card" href="/channels/ussd"><div className="card-icon">☎️</div><div className="card-title">USSD</div><div className="card-desc">Manage your subscription without a data connection.</div></a>
</div>
