export const meta = {
  eyebrow: "API reference",
  title: "Webhook events",
  lede: "Every event type we emit, with payload shape and the signature header that proves it came from us.",
};

<p>This is the reference table for every event type, for the delivery mechanics (retry, dead-letter, replay), see <a href="/developer/webhooks">Webhooks</a>. For verifying the signature on any of these, see <a href="/security/webhook-verification">Webhook signature verification</a>.</p>

<h2 id="h-events">Event catalogue</h2>
<table>
  <tr><th>Event</th><th>`data` shape</th></tr>
  <tr><td>`subscription.created`</td><td>`{ subscriptionId, customerId, planId, status }`</td></tr>
  <tr><td>`subscription.updated`</td><td>`{ subscriptionId, changes: { field: { from, to } } }`</td></tr>
  <tr><td>`subscription.cancelled`</td><td>`{ subscriptionId, cancelledBy: "merchant" | "subscriber", effectiveAt, cancellationReason? }`</td></tr>
  <tr><td>`subscription.renewed`</td><td>`{ subscriptionId, cycleStart, cycleEnd }`</td></tr>
  <tr><td>`invoice.paid`</td><td>`{ invoiceId, subscriptionId, amount, currency }`</td></tr>
  <tr><td>`payment.failed`</td><td>`{ subscriptionId, invoiceId, amount, currency, failureReason }`</td></tr>
  <tr><td>`payment.recovered`</td><td>`{ subscriptionId, invoiceId, amount, currency, recoveredVia: "whatsapp" | "sms" | "email" | "ussd" | "portal" }`</td></tr>
</table>
<p className="body-secondary">`cancellationReason` is only present on `subscription.cancelled` when a merchant has enabled reason capture in the portal, see <a href="/merchants/customer-portal/cancellation-page">Add a cancellation page</a>.</p>

<h2 id="h-envelope">The envelope every event shares</h2>

```json
{
  "id": "evt_01HMFB...",
  "type": "payment.recovered",
  "createdAt": "2026-08-14T11:04:02Z",
  "data": { /* event-specific, see table above */ },
  "merchantId": "mrc_01HK...",
  "apiVersion": "v1"
}
```


<h2 id="h-headers">Delivery headers</h2>

```text
X-Nomba-Subs-Signature: t=1755168153,v1=5d9c8...
X-Nomba-Subs-Webhook-Id: whk_01HMFB...
X-Nomba-Subs-Event-Id: evt_01HMFB...
```

<p>See <a href="/security/webhook-verification">Webhook signature verification</a> for exactly how to check the `X-Nomba-Subs-Signature` header.</p>

<h2 id="h-next">Next</h2>
<div className="card-grid cols-2">
  <a className="card" href="/developer/webhooks"><div className="card-icon">🔔</div><div className="card-title">Webhooks (guide)</div><div className="card-desc">Subscribing, retries, dead-letter, and replay.</div></a>
  <a className="card" href="/security/webhook-verification"><div className="card-icon">✔️</div><div className="card-title">Signature verification</div><div className="card-desc">Code in five languages.</div></a>
</div>
